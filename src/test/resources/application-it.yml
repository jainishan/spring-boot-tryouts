management:
  metrics:
    export:
      influx:
        enabled: false
    endpoint:
      metrics:
        enabled: false
spring:
  datasource:
    url: jdbc:mysql://root:rpass@localhost:3309/test?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8&useLegacyDatetimeCode=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    separator: ^
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: validate
    show-sql: false

